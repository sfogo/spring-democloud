spring:
  profiles: dev
  application:
    name: turbine
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true

---
# A separate profile used when deploying with Docker
spring:
  profiles: docker
  application:
    name: turbine
  cloud:
    config:
      uri: http://config:8888
      fail-fast: true
